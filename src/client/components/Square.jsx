import React from 'react';

const Square = (props) => {
    const styles = {backgroundColor: props.color}
    return (
        <button style={styles} className='square'> 
            
        </button>
    )
}

export default Square;